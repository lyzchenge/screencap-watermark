name: Build DMG

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout patches
        uses: actions/checkout@v4

      - name: Clone original ScreenCapture
        run: git clone --depth 1 https://github.com/sadopc/ScreenCapture.git /tmp/original

      - name: Apply patches
        run: |
          # 复制修改过的文件
          cp -r ScreenCapture/Models/*    /tmp/original/ScreenCapture/Models/
          cp -r ScreenCapture/Services/*  /tmp/original/ScreenCapture/Services/
          cp -r ScreenCapture/Features/*  /tmp/original/ScreenCapture/Features/

      - name: Find and patch PreviewViewModel
        run: |
          PREVIEW_VM=$(find /tmp/original -name "PreviewViewModel.swift" | head -1)
          python3 patch_preview_vm.py "$PREVIEW_VM"

      - name: Add new files to Xcode project
        run: |
          PBXPROJ=$(find /tmp/original -name "project.pbxproj" | head -1)
          sudo gem install xcodeproj --no-doc
          ruby -e '
          require "xcodeproj"
          p = Xcodeproj::Project.open(ARGV[0])
          t = p.targets.find { |t| t.is_a?(Xcodeproj::Project::Object::PBXNativeTarget) }
          abort "no target" unless t
          mg = p.main_group.find_subpath("ScreenCapture", false)
          abort "no group" unless mg
          [
            [mg.find_subpath("Models", true) || mg.new_group("Models"), "ScreenCapture/Models/WatermarkConfig.swift"],
            [mg.find_subpath("Services", true) || mg.new_group("Services"), "ScreenCapture/Services/WatermarkRenderer.swift"],
          ].each do |g,path|
            fp = File.join(ARGV[1], path)
            next unless File.exist?(fp)
            next if t.source_build_phase.files.any? { |bf| bf.file_ref&.path&.include?(File.basename(path)) }
            ref = g.new_file(path)
            t.source_build_phase.add_file_reference(ref)
            puts "  added #{File.basename(path)}"
          end
          p.save' "$PBXPROJ" /tmp/original

      - name: Build
        run: |
          XCODEPROJ=$(find /tmp/original -name "*.xcodeproj" -d 1 | head -1)
          xcodebuild \
            -project "$XCODEPROJ" \
            -scheme ScreenCapture \
            -configuration Release \
            -derivedDataPath /tmp/derived \
            build 2>&1 | tail -30

      - name: Package DMG
        run: |
          APP=$(find /tmp/derived -name "ScreenCapture.app" -type d | head -1)
          echo "App: $APP"
          mkdir -p /tmp/dmg
          cp -R "$APP" /tmp/dmg/
          ln -s /Applications /tmp/dmg/Applications
          hdiutil create \
            -volname "ScreenCapture-Watermark" \
            -srcfolder /tmp/dmg \
            -ov -format UDZO \
            /tmp/ScreenCapture-Watermark.dmg
          ls -lh /tmp/ScreenCapture-Watermark.dmg

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: ScreenCapture-Watermark
          path: /tmp/ScreenCapture-Watermark.dmg
